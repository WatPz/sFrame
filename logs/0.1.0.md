# 更新 Update

1. 添加了 **`sF_HUD`** 模块，以帮助 **程序员** 使用 **面向对象** 思维编写与 <u>`HUDTEXT`</u> 相关的内容

   added **`sF_HUD`** module to help **programmers** use **object-oriented** thinking to write content related to <u>`HUDTEXT`</u>

2. 添加了 **`sF_IDs`** 模块，辅助 **`sF_HUD`** 模块的 <u>`ID`</u> 分配

   Added **`sF_IDs`** module, auxiliary **`sF_HUD`** module <u>`ID`</u> assignment

# 修复 Fixed

1. 移除了 **setPos()** 、 **setAlpha()** 、 **setColor()** 中会产生 *BUGs* 的 **duration** 参数

   Remove the **duration** parameter in **setPos()** , **setAlpha() **, and **setColor()** that causes *BUGs*